import { useEffect,useState } from 'react';
import type { TabId } from '../model';
interface Props { onNavigate:(tab:TabId)=>void; }
export function AppFooter({onNavigate}:Props){const[time,setTime]=useState(()=>new Date().toLocaleTimeString());useEffect(()=>{const timer=window.setInterval(()=>setTime(new Date().toLocaleTimeString()),1000);return()=>window.clearInterval(timer);},[]);return <footer className="footer-shell"><div className="footer-grid"><section><div className="footer-brand">BOLD_CO</div><p>An architectural experiment in unapologetic layout density, tactile grids, absolute shadows, and structural honesty.</p></section><section><div className="micro-label">// NAVIGATION CORES</div><div className="footer-links">{(['home','about','blog','dashboard'] as TabId[]).map((tab,index)=><button key={tab} onClick={()=>onNavigate(tab)}>{String(index+1).padStart(2,'0')}. {tab.toUpperCase()}</button>)}</div></section><section><div className="micro-label">// CURRENT SECTOR STATUS</div><div className="terminal-box"><div>SYS_TIME: {time}</div><div>LOC: PHILADELPHIA_PA</div><div className="pink-text">● SERVER: DIRECT_ESTABLISHED</div></div></section></div></footer>;}
