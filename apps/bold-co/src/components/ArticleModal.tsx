import { useEffect,useRef } from 'react';
import type { BlogPost } from '../model';
interface Props { post:BlogPost; onClose:()=>void; }
export function ArticleModal({post,onClose}:Props){const closeRef=useRef<HTMLButtonElement>(null);useEffect(()=>{const previous=document.activeElement instanceof HTMLElement?document.activeElement:null;closeRef.current?.focus();const onKeyDown=(event:KeyboardEvent)=>{if(event.key==='Escape')onClose();};window.addEventListener('keydown',onKeyDown);return()=>{window.removeEventListener('keydown',onKeyDown);previous?.focus();};},[onClose]);return <div className="modal-backdrop" role="presentation" onMouseDown={event=>{if(event.currentTarget===event.target)onClose();}}><article className="article-modal" role="dialog" aria-modal="true" aria-labelledby="article-title"><div className="modal-head"><span className="terminal-tag">#{post.category}</span><button ref={closeRef} className="close-button physical-offset" onClick={onClose}>[ CLOSE_X ]</button></div><div className="micro-label">{post.date} • {post.readTime}</div><h2 id="article-title">{post.title}</h2><div className="accent-bar" style={{backgroundColor:post.color}}>// SHARP_CONCRETE_GRAPHIC_SYSTEM</div><p className="article-summary">{post.summary}</p><p className="article-body">{post.content}</p><div className="article-meta">AUTHOR: <strong>{post.author}</strong> / ID: {post.id}</div></article></div>;}
