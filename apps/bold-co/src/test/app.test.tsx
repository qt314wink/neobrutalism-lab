import { fireEvent,render,screen,within } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { beforeEach,describe,expect,it } from 'vitest';
import App from '../App';import { INITIAL_BLOGS } from '../data/seed';
describe('BOLD_CO specimen',()=>{beforeEach(()=>localStorage.clear());it('navigates across the four specimen screens',async()=>{const user=userEvent.setup();render(<App/>);expect(screen.getByRole('heading',{name:/WE BUILD UNAPOLOGETIC/i})).toBeInTheDocument();await user.click(screen.getByRole('button',{name:/02\. COMPANY_INFO/i}));expect(screen.getByRole('heading',{name:'WHO_WE_ARE & LOCATION'})).toBeInTheDocument();await user.click(screen.getByRole('button',{name:/03\. JOURNAL/i}));expect(screen.getByRole('heading',{name:'STARK IDEAS & CHRONICLES'})).toBeInTheDocument();await user.click(screen.getByRole('button',{name:/04\. DASHBOARD_CMS/i}));expect(screen.getByRole('heading',{name:'CMS INTERFACE / ADMIN'})).toBeInTheDocument();});it('changes the amplified gallery reel with an operable button',async()=>{const user=userEvent.setup();render(<App/>);await user.click(screen.getByRole('button',{name:/02\. COMPANY_INFO/i}));await user.click(screen.getByRole('button',{name:/SELECT REEL 04.*THE SOUND SYNTH/i}));expect(screen.getByRole('heading',{name:'THE SOUND SYNTH'})).toBeInTheDocument();});it('adds a review to the live feed',async()=>{const user=userEvent.setup();render(<App/>);await user.click(screen.getByRole('button',{name:/02\. COMPANY_INFO/i}));await user.type(screen.getByLabelText(/Your Full Name/i),'Ada Grid');await user.type(screen.getByLabelText(/Unfiltered Comment/i),'Tactile, legible, and impossible to ignore.');await user.click(screen.getByRole('button',{name:/SUBMIT EXPERIENCE REVIEW/i}));expect(screen.getByText(/Tactile, legible, and impossible to ignore/i)).toBeInTheDocument();});it('opens the article dialog, closes with Escape, and restores trigger focus',async()=>{const user=userEvent.setup();render(<App/>);await user.click(screen.getByRole('button',{name:/03\. JOURNAL/i}));const trigger=screen.getAllByRole('button',{name:/READ FULL DEPLOYMENT/i})[0];trigger.focus();await user.click(trigger);const dialog=screen.getByRole('dialog');expect(within(dialog).getByRole('heading',{name:INITIAL_BLOGS[0].title})).toBeInTheDocument();fireEvent.keyDown(window,{key:'Escape'});expect(screen.queryByRole('dialog')).not.toBeInTheDocument();expect(trigger).toHaveFocus();});it('publishes a valid CMS post into the journal and can delete it',async()=>{const user=userEvent.setup();render(<App/>);await user.click(screen.getByRole('button',{name:/04\. DASHBOARD_CMS/i}));await user.type(screen.getByLabelText(/Post Title/i),'Concrete interfaces are memory devices');await user.type(screen.getByLabelText(/Author Transmitter/i),'Test Operator');await user.type(screen.getByLabelText(/Brief Abstract/i),'A testable claim about interface memory.');await user.type(screen.getByLabelText(/Full Article Main Content/i),'Physical visual cues can make state transitions easier to remember.');await user.click(screen.getByRole('button',{name:/DEPLOY TO MAIN JOURNAL FEED/i}));expect(screen.getByRole('status')).toHaveTextContent(/SUCCESS/);expect(screen.getByRole('button',{name:/Delete CONCRETE INTERFACES ARE MEMORY DEVICES/i})).toBeInTheDocument();await user.click(screen.getByRole('button',{name:/03\. JOURNAL/i}));expect(screen.getByRole('button',{name:'CONCRETE INTERFACES ARE MEMORY DEVICES'})).toBeInTheDocument();await user.click(screen.getByRole('button',{name:/04\. DASHBOARD_CMS/i}));await user.click(screen.getByRole('button',{name:/Delete CONCRETE INTERFACES ARE MEMORY DEVICES/i}));await user.click(screen.getByRole('button',{name:/03\. JOURNAL/i}));expect(screen.queryByRole('button',{name:'CONCRETE INTERFACES ARE MEMORY DEVICES'})).not.toBeInTheDocument();});it('reports incomplete CMS submissions inline',async()=>{const user=userEvent.setup();render(<App/>);await user.click(screen.getByRole('button',{name:/04\. DASHBOARD_CMS/i}));await user.click(screen.getByRole('button',{name:/DEPLOY TO MAIN JOURNAL FEED/i}));expect(screen.getByRole('alert')).toHaveTextContent(/All required fields/i);});it('restores valid persisted blogs and survives malformed persistence',async()=>{const user=userEvent.setup();const persisted=[{...INITIAL_BLOGS[0],id:7007,title:'RESTORED FROM LOCAL STORAGE'}];localStorage.setItem('boldco_blogs',JSON.stringify(persisted));const first=render(<App/>);await user.click(screen.getByRole('button',{name:/03\. JOURNAL/i}));expect(screen.getByRole('button',{name:'RESTORED FROM LOCAL STORAGE'})).toBeInTheDocument();first.unmount();localStorage.setItem('boldco_blogs','{broken');render(<App/>);await user.click(screen.getByRole('button',{name:/03\. JOURNAL/i}));expect(screen.getByRole('button',{name:INITIAL_BLOGS[0].title})).toBeInTheDocument();});});
