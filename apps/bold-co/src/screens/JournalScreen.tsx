import { AlertCircle,Eye,LayoutDashboard } from 'lucide-react';
import type { BlogPost,TabId } from '../model';
interface Props{blogs:BlogPost[];onOpenArticle:(post:BlogPost)=>void;onNavigate:(tab:TabId)=>void;}
export function JournalScreen({blogs,onOpenArticle,onNavigate}:Props){return <section className="screen-stack" aria-labelledby="journal-title"><div className="section-heading"><div><span className="micro-label">// THE_BOLD_CO_JOURNAL</span><h1 id="journal-title">STARK IDEAS & CHRONICLES</h1></div><span className="terminal-tag">● DYNAMIC_FEED_ONLINE: {blogs.length} ARTICLES</span></div><div className="journal-grid"><div className="post-list">{blogs.length===0?<div className="empty-state specimen-card"><AlertCircle aria-hidden="true"/><h2>NO ARTICLES IN JOURNAL</h2><p>All posts have been cleared. Deploy a new piece from the CMS.</p><button className="cta lime physical-offset" onClick={()=>onNavigate('dashboard')}><LayoutDashboard aria-hidden="true" size={16}/> GO TO DASHBOARD CMS</button></div>:blogs.map(post=><article className="post-card specimen-card" key={post.id}><div className="post-ribbon" style={{backgroundColor:post.color}}><span className="terminal-tag">#{post.category}</span><span>{post.date} • {post.readTime}</span></div><div className="post-body"><button className="post-title" onClick={()=>onOpenArticle(post)}>{post.title}</button><p>{post.summary}</p><div className="post-footer"><span>AUTHOR / TRANSMITTER: <b>{post.author}</b></span><button className="small-action physical-offset" onClick={()=>onOpenArticle(post)}><Eye aria-hidden="true" size={15}/> READ FULL DEPLOYMENT</button></div></div></article>)}</div><aside className="journal-aside"><article className="manifest-card specimen-card"><h2>// BOLD_CO MANIFESTO</h2><p>“We do not design to blend. We design to divide. We build pages that feel heavy, clickable, tactile, and concrete.”</p><span>DECLARED: ARCH_LAB_V1</span></article><article className="newsletter-card specimen-card"><h2>STAY STARK</h2><span className="micro-label">// RAW ELECTRONIC UPDATES ONCE A WEEK</span><label className="sr-only" htmlFor="newsletter-email">Newsletter email</label><input id="newsletter-email" type="email" placeholder="bold@citizen.xyz"/><button type="button">CONNECT CHRONICLES</button></article></aside></div></section>;}
