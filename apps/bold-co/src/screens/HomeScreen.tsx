import type { CSSProperties } from 'react';
import { ArrowRight } from 'lucide-react';
import type { TabId } from '../model';
interface Props { onNavigate:(tab:TabId)=>void; }
const services=[['01','BRUTALIST WEB ENGINEERING','Pure semantic layouts on ironclad structures. Zero ornamental bloat, instant rendering, and responsive grid systems.','#FFE600'],['02','IDENTITY REBELLION','Stickers, poster grids, heavy monospace typography, and raw layouts designed to establish immediate visual identity.','#FF007A'],['03','HIGH-OCTANE CMS PLATFORMS','Authoring controls modeled like physical switches and control terminals. Type, inspect, deploy.','#A2FF00']] as const;
export function HomeScreen({onNavigate}:Props){return <section className="screen-stack" aria-labelledby="home-title"><div className="hero-grid"><div className="hero-panel specimen-card"><span className="corner-ribbon">// AGGRESSIVE_DIGITAL_ART</span><div><span className="status-chip cyan">EST. 2024 / USA</span><h1 id="home-title">WE BUILD UNAPOLOGETIC <span className="highlight-lime">DIGITAL</span> EXPERIENCES.</h1><p>We reject generic template sameness. BOLD_CO constructs high-impact, physical-feeling interfaces that behave like objects instead of decorative glass.</p></div><div className="hero-actions"><button className="cta lime physical-offset" onClick={()=>onNavigate('about')}>EXPLORE OFFICE <ArrowRight aria-hidden="true" size={20}/></button><button className="cta white physical-offset" onClick={()=>onNavigate('blog')}>READ JOURNAL</button><span className="micro-label">// RAW_CODEBASE: SPECIMEN_001</span></div></div><div className="metric-stack"><article className="metric-card pink specimen-card"><span className="micro-label">// PERFORMANCE RECORD</span><strong>0.14s</strong><b>AVERAGE TIME TO INTERACTIVE</b><code>STAT_STABLE_TEST_ID: #4092</code></article><article className="metric-card cyan specimen-card"><span className="micro-label">// WORK COMPLETED</span><strong>142+</strong><b>SHIPPED WEB INTERFACES</b><code>AUDIT_STATUS: 100% SATISFACTION</code></article></div></div><section><div className="section-heading"><div><span className="micro-label">// HIGH INTENSITY CAPABILITIES</span><h2>WHAT WE SHRED DAILY</h2></div><b>NO SLICK BULLETPOINTS. REAL CODE.</b></div><div className="service-grid">{services.map(([number,title,copy,accent])=><article key={number} className="service-card physical-offset" style={{'--service-accent':accent} as CSSProperties}><div className="service-number">{number}</div><h3>{title}</h3><p>{copy}</p></article>)}</div></section><section className="manifest-cta specimen-card"><div><span className="micro-label">/// INTERACTIVE_STATION_READY</span><h2>WANT TO DESTROY STATIC STANDARD TEMPLATES?</h2><p>Construct a cyber-grid identity whose visual force is backed by explicit interaction mechanics.</p></div><button className="cta black physical-offset" onClick={()=>onNavigate('about')}>ACQUIRE CO-ORDINATES →</button></section></section>;}
